<UserControl x:Class="StocksHelper.Views.AuthenticationView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:StocksHelper.Views"
			 xmlns:vm="clr-namespace:StocksHelper.ViewModels"
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="500"
			 FontSize="24">

	<UserControl.DataContext>
		<vm:AuthenticationViewModel/>
	</UserControl.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="2*"/>
			<RowDefinition Height="2*"/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<DockPanel HorizontalAlignment="Center" VerticalAlignment="Center">
			<TextBlock Text="Логин"/>
			<TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
		</DockPanel>

		<DockPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
			<TextBlock Text="Пароль"/>
			<TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
		</DockPanel>

		<Button Grid.Row="2" Content="Войти в учетную запись" Command="{Binding AuthUserCommand}" FontSize="20" VerticalContentAlignment="Stretch"/>
	</Grid>
</UserControl>
